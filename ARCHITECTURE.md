# 📐 PROJECT ARCHITECTURE DIAGRAM

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER INTERFACE                          │
│                      (Frontend - Browser)                       │
│                                                                 │
│  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐     │
│  │  index.html   │  │   style.css   │  │   script.js   │     │
│  │  (Structure)  │  │   (Styling)   │  │    (Logic)    │     │
│  └───────────────┘  └───────────────┘  └───────────────┘     │
│           │                  │                  │              │
│           └──────────────────┴──────────────────┘              │
│                              │                                 │
└──────────────────────────────┼─────────────────────────────────┘
                               │
                               │ HTTP Requests (JSON)
                               │
┌──────────────────────────────▼─────────────────────────────────┐
│                      BACKEND API SERVER                        │
│                   (Node.js + Express)                          │
│                   Port: 3000                                   │
│                                                                │
│  ┌────────────────────────────────────────────────────────┐   │
│  │                    server.js                           │   │
│  │                                                        │   │
│  │  ┌──────────────┐      ┌──────────────┐             │   │
│  │  │ Middleware   │      │   Routes     │             │   │
│  │  ├──────────────┤      ├──────────────┤             │   │
│  │  │ • CORS       │      │ GET /        │             │   │
│  │  │ • JSON       │      │ POST /gen... │             │   │
│  │  │ • dotenv     │      │ POST /gen... │             │   │
│  │  └──────────────┘      └──────────────┘             │   │
│  │                                                        │   │
│  └────────────────────────────────────────────────────────┘   │
│                              │                                 │
└──────────────────────────────┼─────────────────────────────────┘
                               │
                               │ API Calls (HTTPS)
                               │
┌──────────────────────────────▼─────────────────────────────────┐
│                      OPENAI API                                │
│                   (External Service)                           │
│                                                                │
│  ┌──────────────────┐              ┌──────────────────┐       │
│  │   GPT-4o-mini    │              │    DALL-E 3      │       │
│  │                  │              │                  │       │
│  │ • Generate       │              │ • Generate       │       │
│  │   Articles       │              │   Images         │       │
│  │ • 3 Articles     │              │ • With Text      │       │
│  │ • JSON Format    │              │ • 1024x1024      │       │
│  └──────────────────┘              └──────────────────┘       │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 🔄 DATA FLOW DIAGRAM

### Step 1: Article Generation

```
┌──────────┐
│  User    │  1. Enters topic + preferences
└─────┬────┘
      │
      ▼
┌─────────────────────┐
│  Frontend (JS)      │  2. Validates input
└─────┬───────────────┘
      │
      │ POST /generate-articles
      │ {topic, length, tone, language}
      ▼
┌─────────────────────┐
│  Backend (Express)  │  3. Constructs prompt
└─────┬───────────────┘
      │
      │ OpenAI API Request
      ▼
┌─────────────────────┐
│  GPT-4o-mini        │  4. Generates 3 articles
└─────┬───────────────┘
      │
      │ Returns JSON
      │ {article1, article2, article3}
      ▼
┌─────────────────────┐
│  Backend            │  5. Sends response
└─────┬───────────────┘
      │
      │ JSON Response
      ▼
┌─────────────────────┐
│  Frontend           │  6. Displays 3 cards
└─────┬───────────────┘
      │
      ▼
┌──────────┐
│  User    │  7. Sees articles
└──────────┘
```

### Step 2: Image Generation

```
┌──────────┐
│  User    │  1. Selects article
└─────┬────┘
      │
      ▼
┌─────────────────────┐
│  Frontend (JS)      │  2. Extracts title (LOCAL)
│                     │     No API call needed!
└─────┬───────────────┘
      │
      │ POST /generate-image
      │ {articleText, articleTitle}
      ▼
┌─────────────────────┐
│  Backend (Express)  │  3. Constructs image prompt
│                     │     (includes text overlay)
└─────┬───────────────┘
      │
      │ OpenAI DALL-E API Request
      ▼
┌─────────────────────┐
│  DALL-E 3           │  4. Generates image
│                     │     • Background scene
│                     │     • Text embedded
└─────┬───────────────┘
      │
      │ Returns image URL
      ▼
┌─────────────────────┐
│  Backend            │  5. Sends image URL
└─────┬───────────────┘
      │
      │ JSON Response {imageUrl}
      ▼
┌─────────────────────┐
│  Frontend           │  6. Displays image + download
└─────┬───────────────┘
      │
      ▼
┌──────────┐
│  User    │  7. Downloads image
└──────────┘
```

---

## 🗂️ FILE STRUCTURE & RELATIONSHIPS

```
test4/
│
├── 📄 Documentation Layer
│   ├── START_HERE.md ──────────► Entry point
│   ├── README.md ──────────────► Main docs
│   ├── QUICKSTART.md ──────────► Fast setup
│   ├── API_DOCS.md ────────────► API reference
│   ├── TESTING.md ─────────────► Test guides
│   ├── DEPLOYMENT.md ──────────► Deploy guides
│   ├── PROJECT_SUMMARY.md ─────► Overview
│   ├── FEATURE_CHECKLIST.md ───► Verification
│   └── ARCHITECTURE.md ────────► This file
│
├── 🔧 Automation Layer
│   ├── SETUP.ps1 ──────────────► Auto setup
│   └── RUN.ps1 ────────────────► Auto run
│
├── 🔙 Backend Layer
│   ├── server.js
│   │   ├── Imports ────────────► express, cors, openai
│   │   ├── Middleware ─────────► CORS, JSON parsing
│   │   ├── Routes
│   │   │   ├── GET / ─────────► Health check
│   │   │   ├── POST /generate-articles
│   │   │   └── POST /generate-image
│   │   └── Server.listen ──────► Port 3000
│   │
│   ├── package.json ───────────► Dependencies
│   ├── .env.example ───────────► Config template
│   └── .gitignore ─────────────► Git rules
│
└── 🎨 Frontend Layer
    ├── index.html
    │   ├── Structure ──────────► Sections, forms, buttons
    │   ├── Links to ───────────► style.css, script.js
    │   └── UI Elements ────────► Inputs, cards, image display
    │
    ├── style.css
    │   ├── Layout ─────────────► Grid, flexbox
    │   ├── Styling ────────────► Colors, fonts, spacing
    │   ├── Animations ─────────► Transitions, keyframes
    │   └── Responsive ─────────► Media queries
    │
    └── script.js
        ├── Configuration ──────► API_BASE_URL
        ├── State ──────────────► articles, selection
        ├── DOM Elements ───────► References
        ├── Event Listeners ────► Click handlers
        ├── Functions
        │   ├── generateArticles()
        │   ├── displayArticles()
        │   ├── selectArticle()
        │   ├── extractTitle() ─► LOCAL (no API)
        │   ├── generateImage()
        │   ├── showError()
        │   └── restart()
        └── API Calls ──────────► fetch() to backend
```

---

## 🔌 API ENDPOINT MAP

```
Backend API (http://localhost:3000)
│
├── GET /
│   └── Returns: {message: "API is running"}
│       Status: 200
│       Use: Health check
│
├── POST /generate-articles
│   ├── Input:
│   │   └── {topic, length, tone, language}
│   │
│   ├── Process:
│   │   ├── 1. Validate topic
│   │   ├── 2. Map length to word count
│   │   ├── 3. Construct prompt
│   │   ├── 4. Call OpenAI GPT-4o-mini
│   │   ├── 5. Parse JSON response
│   │   └── 6. Validate structure
│   │
│   └── Output:
│       ├── Success (200): {article1, article2, article3}
│       ├── Error (400): {error: "Topic required"}
│       └── Error (500): {error: "Failed to generate"}
│
└── POST /generate-image
    ├── Input:
    │   └── {articleText, articleTitle}
    │
    ├── Process:
    │   ├── 1. Validate inputs
    │   ├── 2. Construct image prompt
    │   │   └── Includes title as text overlay
    │   ├── 3. Call OpenAI DALL-E 3
    │   └── 4. Extract image URL
    │
    └── Output:
        ├── Success (200): {imageUrl}
        ├── Error (400): {error: "Text and title required"}
        └── Error (500): {error: "Failed to generate"}
```

---

## 🎯 COMPONENT INTERACTION

```
┌─────────────────────────────────────────────────────────┐
│                    USER INTERFACE                       │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌──────────────┐  ┌──────────────┐  ┌─────────────┐  │
│  │   Input      │→ │   Articles   │→ │   Image     │  │
│  │   Section    │  │   Section    │  │   Section   │  │
│  └──────────────┘  └──────────────┘  └─────────────┘  │
│         │                  │                 │         │
│         │                  │                 │         │
│         ▼                  ▼                 ▼         │
│  ┌────────────────────────────────────────────────┐   │
│  │          script.js (Event Handlers)            │   │
│  │                                                │   │
│  │  • generateArticles()                         │   │
│  │  • selectArticle()                            │   │
│  │  • generateImage()                            │   │
│  │  • extractTitle()  ← LOCAL, no API call       │   │
│  └────────────────────────────────────────────────┘   │
│         │                                  │           │
│         ▼                                  ▼           │
│  ┌─────────────┐                   ┌─────────────┐   │
│  │ Fetch API   │                   │ Fetch API   │   │
│  │ (Articles)  │                   │ (Image)     │   │
│  └─────────────┘                   └─────────────┘   │
└─────────┼────────────────────────────────┼───────────┘
          │                                │
          │ API Call 1                     │ API Call 2
          │                                │
┌─────────▼────────────────────────────────▼───────────┐
│              Backend Server (server.js)              │
│                                                       │
│  ┌──────────────────┐        ┌──────────────────┐   │
│  │ /generate-       │        │ /generate-       │   │
│  │  articles        │        │  image           │   │
│  └────────┬─────────┘        └────────┬─────────┘   │
│           │                           │             │
│           ▼                           ▼             │
│  ┌──────────────────┐        ┌──────────────────┐   │
│  │ OpenAI Client    │        │ OpenAI Client    │   │
│  └────────┬─────────┘        └────────┬─────────┘   │
└───────────┼──────────────────────────┼──────────────┘
            │                          │
            ▼                          ▼
   ┌────────────────┐         ┌────────────────┐
   │  GPT-4o-mini   │         │   DALL-E 3     │
   │  (Articles)    │         │   (Image)      │
   └────────────────┘         └────────────────┘
```

---

## 📦 DEPENDENCY TREE

```
Backend Dependencies:
├── express@4.18.2
│   ├── body-parser
│   ├── cookie
│   ├── debug
│   ├── methods
│   └── ... (sub-dependencies)
│
├── cors@2.8.5
│   ├── object-assign
│   └── vary
│
├── dotenv@16.3.1
│   └── (no dependencies)
│
└── openai@4.20.1
    ├── @types/node
    ├── axios
    ├── form-data
    └── ... (sub-dependencies)

Frontend Dependencies:
└── None (Pure vanilla JavaScript)
```

---

## 🔐 SECURITY ARCHITECTURE

```
┌─────────────────────────────────────────────────┐
│               Security Layers                   │
├─────────────────────────────────────────────────┤
│                                                 │
│  Layer 1: Environment Variables                │
│  ├── .env file (not in git)                    │
│  ├── .gitignore protection                     │
│  └── dotenv package                            │
│                                                 │
│  Layer 2: API Key Protection                   │
│  ├── Stored in backend only                    │
│  ├── Never exposed to frontend                 │
│  └── Never in version control                  │
│                                                 │
│  Layer 3: Input Validation                     │
│  ├── Frontend validation                       │
│  ├── Backend validation                        │
│  └── Error handling                            │
│                                                 │
│  Layer 4: CORS Configuration                   │
│  ├── Controls allowed origins                  │
│  ├── Prevents unauthorized access              │
│  └── Configurable for production               │
│                                                 │
│  Layer 5: HTTPS (Production)                   │
│  ├── SSL/TLS encryption                        │
│  ├── Secure API calls                          │
│  └── Certificate management                    │
│                                                 │
└─────────────────────────────────────────────────┘
```

---

## ⚡ PERFORMANCE OPTIMIZATION

```
Optimization Strategy:
│
├── Frontend
│   ├── No framework overhead
│   ├── Minimal CSS (no libraries)
│   ├── Vanilla JS (fast execution)
│   ├── Lazy loading (images)
│   └── Local title extraction
│
├── Backend
│   ├── Efficient Express routing
│   ├── JSON parsing only when needed
│   ├── Async/await for non-blocking
│   ├── Error handling
│   └── Console logging for debugging
│
├── API Calls
│   ├── Only 2 OpenAI calls total
│   ├── GPT-4o-mini (faster, cheaper)
│   ├── Batch article generation
│   ├── Single image generation
│   └── No redundant requests
│
└── Data Transfer
    ├── Compressed JSON responses
    ├── CORS optimized
    ├── Minimal payload sizes
    └── Efficient error responses
```

---

## 🚀 DEPLOYMENT ARCHITECTURE

```
Development:
┌──────────────┐      ┌──────────────┐
│  Localhost   │ ──── │  Localhost   │
│  :3000       │      │  File System │
│  (Backend)   │      │  (Frontend)  │
└──────────────┘      └──────────────┘

Production Option 1 (Cloud):
┌──────────────┐      ┌──────────────┐
│  Heroku      │ ──── │  Vercel      │
│  (Backend)   │      │  (Frontend)  │
└──────────────┘      └──────────────┘

Production Option 2 (VPS):
┌─────────────────────────────┐
│       AWS EC2 / DO          │
│  ┌──────────┐  ┌──────────┐ │
│  │  Node.js │  │  Nginx   │ │
│  │ (Backend)│  │(Frontend)│ │
│  └──────────┘  └──────────┘ │
└─────────────────────────────┘

Production Option 3 (Docker):
┌─────────────────────────────┐
│      Docker Compose         │
│  ┌──────────┐  ┌──────────┐ │
│  │Backend   │  │Frontend  │ │
│  │Container │  │Container │ │
│  └──────────┘  └──────────┘ │
└─────────────────────────────┘
```

---

## 📊 STATE MANAGEMENT

```
Frontend State:
├── generatedArticles = {}
│   ├── article1: "text..."
│   ├── article2: "text..."
│   └── article3: "text..."
│
├── selectedArticle = null | "text..."
│
├── selectedArticleKey = null | "article1|2|3"
│
└── UI State
    ├── loading: hidden | visible
    ├── articlesSection: hidden | visible
    ├── imageSection: hidden | visible
    ├── resultSection: hidden | visible
    └── errorMessage: hidden | visible

Backend State:
├── Server Running: true | false
├── OpenAI Client: initialized
└── Environment Variables: loaded
```

---

## 🎯 EXECUTION FLOW SUMMARY

```
1. User Opens App
   └─► Frontend loads (HTML/CSS/JS)

2. User Enters Topic
   └─► Form captures input

3. User Clicks "Generate Articles"
   ├─► Frontend validates
   ├─► POST to /generate-articles
   ├─► Backend processes
   ├─► OpenAI GPT-4o-mini generates
   ├─► Backend returns JSON
   └─► Frontend displays 3 cards

4. User Selects Article
   ├─► Frontend captures selection
   ├─► Title extracted locally
   └─► Image section appears

5. User Clicks "Generate Image"
   ├─► Frontend sends article + title
   ├─► POST to /generate-image
   ├─► Backend constructs prompt
   ├─► OpenAI DALL-E 3 generates
   ├─► Backend returns image URL
   └─► Frontend displays image

6. User Downloads Image
   └─► Browser downloads from URL

Total Time: ~15-30 seconds
Total API Calls: 2
```

---

**This architecture supports:**
- ✅ Scalability
- ✅ Maintainability
- ✅ Security
- ✅ Performance
- ✅ Cost optimization
- ✅ Easy deployment

---

*Architecture designed for production-ready deployment*
*Last updated: December 11, 2025*
